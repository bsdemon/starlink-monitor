[project]
name = "starlink-monitor"
version = "0.1.0"
description = "Simple Starlink monitoring tool"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "psycopg[binary,extras,pool]>=3.3.3",
    "requests>=2.32.5",
]
